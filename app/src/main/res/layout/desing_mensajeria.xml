<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Mensajeria">

    <!-- Título Principal -->
    <TextView
        android:id="@+id/text_title_mensajeria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mensajería"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_marginTop="24dp"
        android:layout_marginStart="24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 1. RecyclerView para la lista de chats (LISTA LLENA) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_chats_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:paddingHorizontal="4dp"
        android:clipToPadding="false"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation_bar"
        app:layout_constraintTop_toBottomOf="@+id/text_title_mensajeria"
        android:visibility="gone"
        tools:visibility="visible"
        tools:listitem="@layout/desing_item_chat_send" />

    <!-- 2. Contenedor de Estado Vacío (SIN CHATS ACTIVOS) -->
    <LinearLayout
        android:id="@+id/layout_empty_state_chats"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="visible"
        tools:visibility="gone"
        app:layout_constraintTop_toBottomOf="@+id/text_title_mensajeria"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation_bar">

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/ico_cleaning"
            android:visibility="visible"
            android:contentDescription="Icono de Mensajes Vacíos" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="¡Aún no tienes chats activos!"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#374151"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Los chats aparecerán aquí cuando respondas o inicies una solicitud de donación."
            android:textSize="16sp"
            android:textColor="#6b7280"
            android:gravity="center" />
    </LinearLayout>

    <!-- Barra de Navegación Inferior -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/windowBackground"
        app:menu="@menu/menu_navegacion"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>